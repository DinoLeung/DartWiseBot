FROM dart:stable

ARG BOT_TOKEN
ENV BOT_TOKEN=$BOT_TOKEN

ARG HOST_URL
ENV HOST_URL=$HOST_URL

ARG BOT_PORT
ENV BOT_PORT=$BOT_PORT

ARG SERVER_PORT
ENV SERVER_PORT=$SERVER_PORT

ARG MYID
ENV MYID=$MYID

EXPOSE 8443
